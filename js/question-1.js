const getRemainder = (a, b) => a % b;

getRemainder();

// I tested the function by typing 'console.log(getRemainder(5,10));
// I don't know other ways to get a visual response out of a return function,
// except from maybe creating HTML with it. Is there another way to print and test in console?
